import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'app-gestion-siniestros',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
